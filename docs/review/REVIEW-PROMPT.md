你是独立审查者。请对 当前仓库 的全部当前代码做完整、只读审查。请同时检查跟踪与未跟踪文件；不能只看 git diff，也不能把空diff当成没有改动。

请独立阅读源码、运行适当的隔离验证，不沿用前一个Agent或 README/ACCEPTANCE 中“完成/通过”的结论。文档是待核实声明，不是证据。不要修改源码，不要commit，不要部署或重启现有代理，不要发送真实飞书消息。不要读取/输出真实Key或运行真实供应商调用脚本；普通隔离回归不需要真实凭证。你只需交付完整审查报告，交由维护者核对。

任务背景：公司内网Rust单实例Responses网关，文件存储，无数据库。模型下按优先级配置渠道，每渠道可多Key轮询。只改URL、认证Key和请求model，正常响应透传。上游Key及公司Key明文保存是用户明确要求，不把这一点当漏洞。管理员登录密码用Argon2。

功能：New API/Sub2API与现有订阅平台额度检测；DeepSeek/GLM模型名大小写不敏感走官方余额/Coding Plan；质量、额度、服务、凭证独立状态；定时降智监控；故障切换、防抖和有限恢复；飞书事件去重；四栏管理页；最多200条事件日志。

先读 docs/PLAN.md、docs/ERROR-HANDLING.md、README.md、docs/ACCEPTANCE.md，再独立核对 src/、static/、scripts/ 全部文件。无需对已有参考项目作修改。

重点检查：
1. 错误分类：HTTP200业务错误（智谱假Key）、嵌套/顶层code、403余额不足、429周期耗尽与限流、未知4xx/5xx、HTML/空响应/非法JSON、SSE首帧与中途错误，是否漏判/误判正常回答或用户输入错误。
2. 有限重试：不同URL/模型/Key去重，600秒切换预算，不设8候选上限，无重复候选；并发下仍会不会反复选择坏Key；恢复12次上限、疑似故障确认、无冷却阻塞、手动验证、重启和配置变更是否可绕过或永久卡死。
3. 透传：请求未知字段、HTTP头、非SSE正文、SSE分片/大帧/多行、取消、终态，是否丢内容或拼接两个供应商回答；是否因重试重复计费或执行工具；既定previous_response_id/conversation明确拒绝边界是否合理且与文档一致。
4. 额度与状态：无限Token与账号余额、订阅到期/重置、GLM剩余比例、未知/过期质量；异步旧查询覆盖新故障、配置编辑/删除后的缓存和竞态；错误作用范围是Key、账号、渠道还是模型，有没有错误扩散。
5. 内存与并发：业务请求无大小/并发/聚合内存配额，核查非SSE整包解析及大请求内存增长、SSE观察缓冲、日志/通知/监控历史、请求取消、连接池、检查去重、许可生命周期、文件读取/写入；不要把Rust或有限RSS测试当成无泄漏证明。
6. 管理页面与API：保存后状态一致性、并发编辑、平台自动识别、未知平台、Key回显、配置验证、错误提示、手动操作是否真执行；密码登录、CSRF/凭证隔离、权限、文件完整性。
7. 调度与通知：上海时间分段边界、检查超时、慢任务、公平性、恢复与通知去重、队列上限、重启持久化、消息投递失败；不能误报成功或无限刷通知。
8. 测试有效性：现有断言是否真正覆盖这些行为；找遗漏并尽量提供最小可重复步骤。不要仅列测试数量或编译结果。

可运行：cargo fmt --check；cargo clippy --all-targets -- -D warnings；cargo test --locked；cargo build --locked；python3 scripts/check_http.py；cargo build --release --locked；python3 scripts/check_memory.py。浏览器测试 scripts/check_ui.cjs 需要 Playwright，缺依赖则明确未运行，不伪称通过。不要运行 probe_providers.py、check_live.py、check_gateway_live.py（会调用真实账号）。独立验证临时脚本放仓库外，完成清理。

报告格式：
- 先给结论：是否可部署、是否有阻塞项。
- Findings按P0/P1/P2/P3排序。每项给文件绝对路径+行号、具体触发条件、完整因果链、影响、可重复证据、建议修复；区分已复现与源码推断。没有问题的方面不用凑finding，不把纯偏好当缺陷。
- 给“需求/规则 → 对应实现 → 验证方式 → 已验证/有缺陷/未验证”的覆盖表。
- 列实际执行的命令和结果，任何失败/未运行/外部依赖都说明。
- 最后列残余风险和需要补测的边界。发现严重问题仍完成其余范围审查，不要找到一个问题就停。

只输出审查报告；不要直接替我修代码。

补充：核查员工Key新增/停用/删除及总Key保留、鉴权、usage去重与跨候选合计、上海自然日五日窗口、缺失用量和取消、持久化、敏感信息隔离。
