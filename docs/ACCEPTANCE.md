# 验收记录

日期：2026-09-22。测试环境：本机 macOS / Apple Silicon，Rust 1.98.1。没有停止或重启其他既有代理服务，没有提交 Git，没有向公司真实飞书 Webhook 发消息。

## 2026-09-23 审查修复回归

本轮不调用真实供应商/真实飞书。两份审查逐项结论见 `review/RESOLUTION.md`。

- `cargo fmt --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test --locked`（25 passed）通过；debug/release均重新构建。
- 扩展 `scripts/check_http.py` 通过：70KB data-only SSE终态、终态别名、未识别终态、delta和failed同/分片一致性、11秒首帧、9MiB本地超限、非白名单400/422、合法code:ok/error:{}、手动并发409、配置版本冲突、模型改名故障保留、手改无关配置后重启、自动探测429不耗预算、改密撤销会话；同时保留原路由/额度/本地通知回归。
- `scripts/check_ui.cjs`通过，使用 `PLAYWRIGHT_MODULE=/path/to/node_modules/playwright`；登录、模型/渠道创建、平台识别、保存、明文Key、绿框、日志、设置均正常，无JS异常。该路径是本机依赖位置，不要求其他环境相同。
- 发布版内存回归2200请求+200取消通过：RSS空闲8672KiB，400请求15536，800请求15616，1200/1600请求15824，2000请求/取消后/稳定15952。最后的启动报错、EOF分支及请求错误文案微调后重新构建；没有重复整个内存负载。
- 独立临时启动检查：损坏/超大config与state均退出码1，原文件完整保留。
- 未做Docker构建、真实Webhook或生产部署；没有把有限回归解释为所有环境安全保证。

## 后续功能验收

后续加入配置中心布局、渠道拖动排序、提供者/Key开关、监控时间段配置、GET /v1/models和--port启动参数。当前Rust测试27项；详细命令以README为准。

## 原始功能证据

- 原始验收测试计数已过时；本轮 Rust 单元测试25/25通过：额度解析、无限 Token 负数、GLM 周期与剩余比例、订阅到期、大小写识别、故障周期合并、递增探测间隔、恢复间隔、防抖冷却、200条事件上限、SSE 分片/多行/大帧终态、真实供应商错误分类。
- 隔离 HTTP 回归：管理员登录、公司 Key、配置保存与平台验证、请求字段及认证替换、SSE透传、503自动切换、故障候选跳过、未知模型、上游私有历史字段拒绝、手动验证、429余额错误识别、删除渠道缓存清理、全部不可用503。
- 同一 HTTP 回归还验证：200 SSE早期失败切换；监控满血→降智→恢复与路由联动；本地模拟飞书收到一次降智通知，持续降智不重复；重启保留故障状态；手动重置后实际探测恢复。
- Playwright：登录、新建模型、添加渠道并识别额度、保存、明文 Key 回显、当前路由绿框、日志与设置。没有页面 JS 异常。截图在被忽略的 `.runtime/ui-codex.png` 和 `.runtime/ui-narrow.png`。

## 真实供应商验证边界

曾在授权环境验证平台额度接口、Responses和质量检测。公开仓库不包含账号归属、Key数量、余额或原始响应；本地原始证据仅在忽略目录保存，不随仓库分发。真实接口行为随供应商而变，不能将历史结果视为当前健康保证。

## 内存与取消

发布版、16并发、每个普通请求约32KiB输入，2200个完整请求，加200个主动中断的流。

| 时点 | RSS KiB |
|---|---:|
| 空闲 | 8640 |
| 400请求 | 15088 |
| 800请求 | 15232 |
| 1200请求 | 15328 |
| 1600请求 | 15344 |
| 2000请求 | 15360 |
| 200次取消后 | 15360 |
| 再完成200请求并稳定 | 15408 |

取消后仍可处理超过并发许可总数的新请求，未出现许可耗尽；该工作负载后半段RSS趋稳。这个有限时长测试未发现持续增长，不能据此保证所有生产负载绝无内存泄漏。

## 部署边界

- 正式密码、公司Key、飞书Webhook尚由管理员部署时提供；首次启动环境变量完成初始化，页面可改密码/Key/Webhook。
- 没有部署到公司服务器；本机发布二进制已构建。Dockerfile提供Linux构建方案，Docker镜像尚未在本机构建。
- 飞书HTTP投递使用本地模拟接收器验证，未验证公司的真实Webhook。
- 开始向客户端返回后不能无痕拼接另一个供应商回答。
- 引用供应商服务端历史的 `previous_response_id` / `conversation` 不可安全跨站，返回明确400；普通完整历史透传。
- New API无限Key的账户余额未知，实际报欠费后须探测成功才解除，不伪报充值到账。
